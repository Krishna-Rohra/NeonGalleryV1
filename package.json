{
  "name": "neon-gallery",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "deploy": "npm run build && git add dist && git commit -m 'Build for GitHub Pages' && git subtree push --prefix dist origin gh-pages"
}
,
  "devDependencies": {
    "vite": "^7.0.4",
    "vite-plugin-pages": "^0.33.1"
  },
  "dependencies": {
    "appwrite": "^18.2.0"
  }
}
